spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mavidenergy?useSSL=false&serverTimezone=UTC
    username: root
    password: shhummer10@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
