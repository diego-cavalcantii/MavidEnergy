spring:
  datasource:
    url: jdbc:mysql://host.docker.internal:3306/mavidenergy?useSSL=false&serverTimezone=UTC
    username: root
    password: sua_nova_senha
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
          ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect